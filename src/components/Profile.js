import React from "react";
import { CheckUserFrmLs } from "../utils";

const Profile = () => {
  CheckUserFrmLs();

  return <h1 className="text-3xl font-bold text-yellow-600">Profil</h1>;
};

export default Profile;
